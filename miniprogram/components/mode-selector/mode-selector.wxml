<view class="mode-group">
  <view
    wx:for="{{modeList}}"
    wx:key="id"
    class="mode-btn {{current === item.id ? 'active' : ''}}"
    data-mode="{{item.id}}"
    bindtap="onTap"
  >
    {{item.label}}
  </view>
</view>
