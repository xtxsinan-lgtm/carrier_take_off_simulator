---
description: 功能改动必须同步 HTML / 小程序 / API / catalog，禁止只改一端
alwaysApply: true
---

# 多发布通道同步

本仓库有多条发布通道。**任何新模式、机型、策略或仿真行为**，改完后必须确认下列通道一致，不得只改 Python 或只改小程序就结束。

## 通道清单

1. **仿真核心**：`simulators/`、`utils/`、`apps/web_simulator.py`
2. **模式目录**：`scripts/frontend_catalog.py` 的 `MODES` / 策略表（Web `data.json` 与小程序同源）
3. **GitHub Pages HTML**：`docs/`（`build_all.py` 生成 `data.json`；模式按钮由 JS 读 `data.modes`，勿在 `index.html` 硬编码模式）
4. **微信小程序**：`miniprogram/`（`build_all.py`；模式列表来自 catalog）
5. **小程序 API**：`apps/miniprogram_api.py`（仅小程序；契约与 `web_simulator` 一致）
6. **iOS App**：`ios/CarrierTakeOff/`（本地 Pyodide + Bundle `data.json`/`py_sources`；无 HTTP 后端）

## 必做

- 改 CSV / 仿真 / catalog 后运行 `python3 scripts/build_all.py`
- 改 HTML 静态资源时同步递增 `docs/js/app.js` 的 `APP_VERSION` 与 `docs/index.html` 里 `app.js?v=`
- push 后确认 GitHub Pages 已刷新（常 1–3 分钟）；若线上仍是旧版，再 bump 版本并 push
- 跑 `python3 -m pytest tests/ -m "not e2e" -q`（含 catalog / HTML / iOS 同步测试）
